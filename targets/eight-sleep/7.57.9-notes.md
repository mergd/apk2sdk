# Eight Sleep 7.57.9 SDK update

APKPure: https://apkpure.net/eight-sleep/com.eightsleep.eight/download
Package/versionCode: `com.eightsleep.eight` / `1007057009`
XAPK SHA-256: `8c9daa1d5a0134ca869ab0c529ca57ab728b7ce8e0ae7174c90dc8e7bddfaf79`
Signing certificate SHA-1: `0cd030c51e95985cb268fe20d46eca0902cc4bdf`

The decompiled Retrofit alarm interface calls `app-api /v2/users/{id}/alarms` for reads. An authorized test account returned HTTP 200 there and HTTP 404 from the SDK's previous `client-api /v1/users/{id}/alarms` route. The v2 response adds `recommendedAlarm`. Subscription reads remain on `app-api /v3/users/{id}/subscriptions`, while temperature/all and Autopilot details remain on app API v1. SDK read calls for current user, temperature status, trends, device, and household schedule succeeded.

This change routes alarm reads to app API v2 and lets raw requests select v2 or v3 without constructing another client. It also corrects native alarm types and sends create/update/delete/snooze/dismiss through the app API methods shown by the APK. Those alarm writes have contract tests but were not exercised against a Pod. The test account is premium, so these observations do not prove free-tier access.
