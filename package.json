{
  "name": "apk2sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/beli build && bun run --cwd packages/eight-sleep build && bun run --cwd packages/segway build && bun run --cwd packages/butterflymx build",
    "test": "bun test",
    "typecheck": "bun run --cwd packages/beli typecheck && bun run --cwd packages/eight-sleep typecheck && bun run --cwd packages/segway typecheck && bun run --cwd packages/butterflymx typecheck",
    "check": "bun run typecheck && bun test && bun run build",
    "pack:beli": "bun run --cwd packages/beli pack",
    "pack:eight-sleep": "bun run --cwd packages/eight-sleep pack",
    "pack:segway": "bun run --cwd packages/segway pack",
    "pack:butterflymx": "bun run --cwd packages/butterflymx pack"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.3.11"
}
